export default function Footer({ phone }) {
    // anything before the return statement
    // is what i call the cool zone
    // it is your last chance for easy javascript before the HTML starts.
    // otherwise, you'll need to user a {} portal to to do js
    // this is where i like to console.log
  return <p style={{ background: 'cyan' }}>Contact me at {phone}</p>;
}
  
// two curlies: the first curly is our portal into js.
// our second portal is a javascript object